// Expected Output
/**
 *
 * return
 */
const useLectureType = (typeId) => {
  const types = {
    1: 'On Live',
    2: 'New',
    3: 'Planning to live',
    4: 'Finished live',
    5: 'Other on Demand',
  };

  const typeContent = {
    onLive: {},
  };
};

export default useLectureType;
